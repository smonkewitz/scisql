
# this really needs to be completely reworked very soon...


gcc -fno-stack-protector -DMYSQL_DYNAMIC_PLUGIN -fPIC -Wall -pedantic -I /usr/include/mysql -c median.c -o median.o
ld -shared -o median.so median.o


# sudo cp -p median.so /usr/lib/mysql/plugin/
# mysql -e "CREATE AGGREGATE FUNCTION median RETURNS REAL SONAME 'median.so'"
