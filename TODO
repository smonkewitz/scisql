
- Allow clock-wise vertex ordering in spherical convex polygon specs
- Implement the test command in the waf build system
- Consider creating temp file for median() and select() in the MySQL TMP_DIR instead of /tmp

